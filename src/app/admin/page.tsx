import type { Metadata } from "next";
import Link from "next/link";
import { redirect } from "next/navigation";
import { SignOutButton } from "@/components/sign-out-button";
import { addBlogPost, addPortfolioEntry, updateBookingStatus } from "./actions";
import { createClient, hasPublicSupabase, requireAdmin, requireUser } from "@/lib/supabase/server";

export const metadata: Metadata = { title: "Studio Admin", robots: { index: false, follow: false } };
const demoBookings = [{ id: "1", reference: "AIT-8F31A2C4", customer_name: "Riya Shah", email: "riya@example.com", tattoo_style: "Fine Line", artist_slug: "mira", appointment_date: "2026-09-12", appointment_time: "15:00:00", status: "confirmed", deposit_amount: 100000 },{ id: "2", reference: "AIT-C52D9B11", customer_name: "Dev Patel", email: "dev@example.com", tattoo_style: "Realism", artist_slug: "vishal", appointment_date: "2026-09-14", appointment_time: "17:00:00", status: "pending_payment", deposit_amount: 100000 },{ id: "3", reference: "AIT-18BE774A", customer_name: "Neha Mehta", email: "neha@example.com", tattoo_style: "Mandala", artist_slug: "aarav", appointment_date: "2026-09-18", appointment_time: "13:00:00", status: "confirmed", deposit_amount: 100000 }];

export default async function AdminDashboard() {
  const configured = hasPublicSupabase(); const user = await requireUser(); const admin = await requireAdmin();
  if (configured && !user) redirect("/login"); if (configured && !admin) redirect("/dashboard");
  let bookings = demoBookings; let portfolioCount = 9; let postCount = 3;
  if (configured) { const supabase = await createClient(); const [bookingsResult, portfolioResult, postsResult] = await Promise.all([supabase.from("consultation_bookings").select("id,reference,customer_name,email,tattoo_style,artist_slug,appointment_date,appointment_time,status,deposit_amount").order("appointment_date", { ascending: true }).limit(30), supabase.from("portfolio_entries").select("id", { count: "exact", head: true }), supabase.from("blog_posts").select("id", { count: "exact", head: true })]); bookings = bookingsResult.data ?? []; portfolioCount = portfolioResult.count ?? 0; postCount = postsResult.count ?? 0; }
  const confirmed = bookings.filter((item) => item.status === "confirmed" || item.status === "completed"); const revenue = confirmed.reduce((sum, item) => sum + item.deposit_amount, 0) / 100;
  return <div className="admin-shell"><header className="admin-top"><div><p className="eyebrow gold-text">Studio control room</p><h1>Good morning.</h1><p>Here’s what is happening at Ahmedabad Ink.</p></div><div>{!configured && <span className="preview-pill">Preview data</span>}{configured && <SignOutButton />}</div></header><nav className="admin-nav"><a href="#overview">Overview</a><a href="#bookings">Bookings</a><a href="#content">Content studio</a><Link href="/">View website ↗</Link></nav><section className="metric-grid" id="overview"><article><span>Upcoming consultations</span><strong>{bookings.length}</strong><small>Next 30 days</small></article><article><span>Deposits collected</span><strong>₹{revenue.toLocaleString("en-IN")}</strong><small>{confirmed.length} confirmed</small></article><article><span>Portfolio pieces</span><strong>{portfolioCount}</strong><small>Published work</small></article><article><span>Blog drafts</span><strong>{postCount}</strong><small>Content pipeline</small></article></section><section className="admin-panel" id="bookings"><div className="admin-heading"><div><p className="eyebrow gold-text">Booking desk</p><h2>Appointments</h2></div><Link className="line-link" href="/book">Create booking ↗</Link></div><div className="admin-table"><div className="table-row table-head"><span>Client</span><span>Appointment</span><span>Artist / style</span><span>Deposit</span><span>Status</span></div>{bookings.map((booking) => <div className="table-row" key={booking.id}><span><strong>{booking.customer_name}</strong><small>{booking.reference}<br />{booking.email}</small></span><span>{formatDate(booking.appointment_date)}<small>{formatTime(booking.appointment_time)}</small></span><span>{capitalize(booking.artist_slug)}<small>{booking.tattoo_style}</small></span><span>₹{(booking.deposit_amount / 100).toLocaleString("en-IN")}</span><span>{configured ? <form action={updateBookingStatus}><input type="hidden" name="id" value={booking.id} /><select name="status" defaultValue={booking.status}><option value="pending_payment" disabled>Pending</option><option value="confirmed">Confirmed</option><option value="completed">Completed</option><option value="cancelled">Cancelled</option></select><button>Save</button></form> : <i className={`status status-${booking.status}`}>{booking.status.replace("_", " ")}</i>}</span></div>)}</div></section><section className="content-studio" id="content"><div className="admin-heading"><div><p className="eyebrow gold-text">CMS</p><h2>Content studio</h2></div><p>Publish new work and prepare studio stories without touching code.</p></div><div className="cms-grid"><CmsCard number="01" title="Portfolio manager" text="Add a new tattoo to the public gallery." disabled={!configured}><form action={addPortfolioEntry}><input name="title" placeholder="Piece title" required /><select name="category" required><option value="">Choose style</option>{["Realism","Fine Line","Mandala","Portrait","Geometric","Minimal","Blackwork","Cover Up"].map((item) => <option key={item}>{item}</option>)}</select><input name="imageUrl" type="url" placeholder="https:// image URL" required /><button className="button gold">Publish work</button></form></CmsCard><CmsCard number="02" title="Blog manager" text="Create an article draft for review." disabled={!configured}><form action={addBlogPost}><input name="title" placeholder="Article title" required /><input name="slug" placeholder="article-url-slug" required /><textarea name="excerpt" placeholder="Short introduction" rows={4} required /><button className="button gold">Save draft</button></form></CmsCard><CmsCard number="03" title="Artist manager" text="Artist records are ready in the database for schedules, profiles and specialties." disabled={!configured}><div className="cms-coming"><strong>3</strong><span>Active artists</span><small>Full profile editor activates with Supabase.</small></div></CmsCard></div></section></div>;
}

function CmsCard({ number, title, text, disabled, children }: { number: string; title: string; text: string; disabled: boolean; children: React.ReactNode }) { return <article className={disabled ? "cms-card disabled" : "cms-card"}><span>{number}</span><h3>{title}</h3><p>{text}</p>{children}{disabled && <small className="preview-lock">Connect Supabase to activate</small>}</article>; }
function capitalize(value: string) { return value === "studio" ? "Studio match" : value.charAt(0).toUpperCase() + value.slice(1); }
function formatDate(value: string) { return new Date(`${value}T00:00:00`).toLocaleDateString("en-IN", { day: "2-digit", month: "short", year: "numeric" }); }
function formatTime(value: string) { const [hour, minute] = value.split(":").map(Number); return new Intl.DateTimeFormat("en-IN", { hour: "numeric", minute: "2-digit", hour12: true }).format(new Date(2020, 0, 1, hour, minute)); }
